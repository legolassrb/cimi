import PageStub from "@/components/PageStub";

export default function AboutPage() {
  return <PageStub title="Statistics / About" description="Curated trust numbers — years in operation, procedures done, etc." />;
}
