import PageStub from "@/components/PageStub";

export default function BookPage() {
  return <PageStub title="Book Appointment" description="Multi-step booking wizard: service → doctor → slot → confirm." />;
}
